#--------------------------------Задача-------------------------------------#
# дан  список
# найти минимальный элемент в списке

l = [1, 5, 8, -5, 9, -3]
if l:
  max_el = l[0]
for element in l:
    if element < max_el:
        max_el = element
print(max_el)
